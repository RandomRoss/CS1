#include <iostream>
#include <cmath>
using namespace std;
// in class activity 3 function called sum from to
double presentValue(double F, double r, double n){ 
double denominator = 1; 
for (int i;i <= n; ++i){
    denominator = (1 + r) * denominator;
}
double P = 0;
P = F/denominator;
    return P;
}
int main(){
cout << presentValue(5000,.005,5) << endl;
    return 0;
}
